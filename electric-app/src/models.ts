export interface FormValues {
  data: {
    ref: string
    location: string
    contact: string
    worker: string
    regNum: number
    phoneNum: number
    email: string
  }
  isChecked: boolean
}
